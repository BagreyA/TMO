matlab
% Параметры сигнала
f = 6; % Частота в Гц
A = 4; % Амплитуда
phi = pi/3; % Фаза

% Временные параметры
t = 0:0.001:1; % Временной вектор от 0 до 1 секунды с шагом 0.001

% Генерация сигнала
y = A * sin(2 * pi * f * t + phi);

% Визуализация сигнала
figure;
plot(t, y);
title('Непрерывный сигнал y(t) = 4sin(2\pi ft + \pi/3)');
xlabel('Время (с)');
ylabel('Амплитуда');
grid on;
axis tight; % Подгоняет оси к данным сгенерировать и визуализировать (вывести на график) непрерывный сигнал используя matlab:𝑦(𝑡) = 4 sin (2𝜋𝑓𝑡 +𝜋/3) , 𝑓 = 6